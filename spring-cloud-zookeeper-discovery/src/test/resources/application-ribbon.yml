spring:
  application:
    name: ribbonApp
  cloud:
    zookeeper:
      discovery:
        metadata:
          testMetadataKey: testMetadataValue
        uriSpec: "{scheme}://{address}:{port}/contextPath"