spring:
  application:
    name: ribbonApp
  cloud:
    zookeeper:
      discovery:
        metadata:
          testMetadataKey: testMetadataValue
management:
  security:
    enabled: false
logging:
  level:
    org.apache.zookeeper.ClientCnxn: ERROR
