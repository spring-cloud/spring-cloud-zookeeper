server:
  port: 8080

#TODO: figure out why I need this here and in bootstrap.yml
spring:
  application:
    name: testZookeeperApp

endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true
  health:
    sensitive: false